syntax = "proto3";

package api;

message PingMessage {
  string ping = 1;
}

message PongMessage {
  string pong = 1;
}

service Status {
  rpc Ping(stream PingMessage) returns (stream PongMessage);
}
