syntax = "proto3";

package marabunta;

message PingMessage {
  string msg = 1;
}

message PongMessage {
  string msg = 1;
}

service Status {
  rpc Ping(stream PingMessage) returns (stream PongMessage);
}
